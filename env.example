# Flask 서버 설정
FLASK_HOST=0.0.0.0
FLASK_PORT=5001
FLASK_DEBUG=false

# 호스트 포트 (docker-compose에서 사용)
HOST_PORT=5001

# 데이터 저장 경로
DATA_DIR=/app/data

# 외부 AI API 설정 (실제 AI 서버 주소로 변경하세요)
EXTERNAL_API_BASE=http://your-ai-server:8000
AI_API_TIMEOUT=10

# 주의: 실제 운영에서는 이 파일을 .env로 복사하고 
# 실제 AI 서버의 주소와 포트로 수정하세요. .env 파일은 git에서 제외됩니다. 